<UserControl x:Class="Sprout.Core.Views.Controls.ConnectionStringBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Sprout.Core.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   x:Name="lblTitle"
                   Text="SQL Server Connection String"
                   FontWeight="Bold"
                   Foreground="DarkSlateGray"
                   Margin="0,0,0,3"/>

        <TextBox Grid.Row="1"
                 Grid.Column="0"
                 x:Name="textBox"
                 VerticalContentAlignment="Center"
                 BorderThickness="0.8"
                 BorderBrush="DarkSlateGray"
                 Text="{Binding ConnectionString, RelativeSource={RelativeSource AncestorType=local:ConnectionStringBox}, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="1"
                Grid.Column="1"
                x:Name="btnBuild"
                Content="…"
                Width="28"
                Margin="4,0,0,0"
                ToolTip="Build connection string"
                Click="BtnBuild_Click"/>
    </Grid>
</UserControl>
