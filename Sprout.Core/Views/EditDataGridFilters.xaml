<UserControl x:Class="Sprout.Core.Views.EditDataGridFilters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sprout.Core.Views"
             xmlns:config="clr-namespace:Sprout.Core.Models.Configurations.DataGrid"
             xmlns:vm="clr-namespace:Sprout.Core.ViewModels"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:EditPageVM}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.7*"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox x:Name="FilterConfigs"
                     Grid.Column="0"
                     Grid.Row="0"
                     SelectedItem="{Binding SelectedFilter}"
                     ItemsSource="{Binding SelectedDataGrid.DataAdapter.DataProvider.FilterConfigs}">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="1" Orientation="Horizontal" FlowDirection="RightToLeft">
                <Button Content="Add"
                        Margin="5,5,0,5"
                        Padding="5"
                        Command="{Binding AddFilterCommand}"/>
                
                <Button Content="Remove"
                        Margin="5,5,0,5"
                        Padding="5"
                        Command="{Binding RemoveFilterCommand}"/>
            </StackPanel>
        </Grid>

        <Grid Name="Content" Grid.Column="1">
            <StackPanel>
                <TextBlock Text="Title"/>
                <TextBox Text="{Binding SelectedFilter.Title, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Text"/>
                <TextBox Text="{Binding SelectedFilter.Text, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="EditorType"/>

                <ComboBox ItemsSource="{Binding EditorTypes}"
                          SelectedItem="{Binding SelectedFilter.EditorType, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
