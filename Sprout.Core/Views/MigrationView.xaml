<UserControl x:Class="Sprout.Core.Views.MigrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Sprout.Core.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:MigrationVM}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Margin="20" VerticalAlignment="Top">
            <TextBlock Text="Migrations"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="DarkSlateGray"
                       Margin="0,0,0,15"/>

            <Button Content="Run Migrations"
                    Command="{Binding RunMigrationsCommand}"
                    HorizontalAlignment="Left"
                    Padding="20,5"/>
        </StackPanel>

        <!-- Busy overlay -->
        <Grid Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Rectangle Fill="White" Opacity="0.6"/>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Executing scripts, please wait…"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" Height="6" Width="220"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
