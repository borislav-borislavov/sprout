
UPDATE Users
SET Username = {@Username},
UserTypeID = {@UserTypeID},
PasswordHash = CASE WHEN PasswordHash <>  {@PasswordHash} THEN HASHBYTES('SHA2_256', CAST({@PasswordHash} AS NVARCHAR(200))) ELSE PasswordHash END
WHERE UserID = {@UserID}

SELECT 1 AS Result

SELECT * FROM Users

