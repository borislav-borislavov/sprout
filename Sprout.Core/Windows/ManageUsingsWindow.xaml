<Window x:Class="Sprout.Core.Windows.ManageUsingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sprout.Core.Windows"
        xmlns:viewModels="clr-namespace:Sprout.Core.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:ManageUsingsVM}"
        Title="Additional Usings"
        Height="420" Width="460"
        WindowStartupLocation="CenterOwner"
        Background="#1F1F1F">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Additional namespaces for this page"
                   FontWeight="Bold"
                   Foreground="#D4D4D4"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
                   Text="One namespace per line, e.g. System.Text"
                   Foreground="#8A8A8A"
                   FontSize="11"
                   Margin="0,0,0,8"/>

        <TextBox Grid.Row="2"
                 Text="{Binding UsingsText, UpdateSourceTrigger=PropertyChanged}"
                 Background="#1C1C1C"
                 Foreground="#D4D4D4"
                 CaretBrush="#D4D4D4"
                 BorderBrush="#3C3C3C"
                 BorderThickness="1"
                 FontFamily="Cascadia Code, Consolas, Courier New"
                 FontSize="13"
                 Padding="8,6"
                 AcceptsReturn="True"
                 AcceptsTab="False"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="OK"
                    Width="90"
                    Height="28"
                    IsDefault="True"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
