<Window x:Class="Sprout.Core.Windows.AddControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sprout.Core.Windows"
        xmlns:viewModels="clr-namespace:Sprout.Core.ViewModels"
        
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:AddControlVM}"
        Title="AddControl"
        WindowStartupLocation="CenterScreen"
        Height="250"
        Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="0"
            Margin="5"
            Content="Control Type:"/>

        <ComboBox
                Grid.Row="0"
                Grid.Column="1"
                MinHeight="30"
                Margin="5"
                SelectedItem="{Binding SelectedControlType}"
                ItemsSource="{Binding AvailableControls}">
        </ComboBox>

        <Label
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            Content="Control Name:"/>

        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Text="{Binding ControlName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            MinHeight="30"
            Margin="5"/>

        <StackPanel
            Orientation="Horizontal"
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            FlowDirection="RightToLeft">

            <Button
                Margin="5"
                Padding="10,5"
                Content="Cancel"
                />

            <Button
                Margin="5"
                Padding="10,5"
                Content="Add"
                Command="{Binding AddControlCommand}"/>
        </StackPanel>
    </Grid>
</Window>
